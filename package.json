{
  "name": "RozCalculate",
  "version": "1.0.0",
  "description": "Roz Calculate",
  "main": "index.js",
  "scripts": {
    "start": "echo 'Running...' && exit 0",
    "build": "mkdir -p dist && cp index.html dist/ 2>/dev/null || true && cp -r src dist/ 2>/dev/null || true && cp *.js dist/ 2>/dev/null || true && cp *.css dist/ 2>/dev/null || true"
  },
  "dependencies": {}
}

